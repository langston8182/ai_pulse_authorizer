{
  "name": "ai_pulse_authorizer",
  "type": "module",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "bestzip build.zip * --ignore=node_modules/aws-sdk/* --ignore=.git",
    "deploy": "npm run build && echo 'Deploying zip file...'"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "aws-jwt-verify": "^5.0.0",
    "bestzip": "^2.2.1"
  }
}
